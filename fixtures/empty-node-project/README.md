# empty-node-project (fixture)

Minimal Node fixture for `genetic-ai-starter` install tests.

**Canonical files:** `package.json`, `src/index.ts`, this README, `.gitignore`.

Run tests from kit root: `node tests/install.test.mjs` — installs kit here then cleans artifacts listed in `.gitignore`.
