# 07b — Host + sell plane

Chains `sdk.hosting.quickStart` then `sdk.hosting.getProjectPlane` for `next_actions[0]`.

```bash
npm run recipe:07b-host-sell
```

Env: same as `07-hosting` plus optional `HOSTING_DRY_RUN=1`.
