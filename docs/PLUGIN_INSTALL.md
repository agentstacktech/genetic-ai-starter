# AgentStack Cursor plugin (consumer projects)

1. Install **AgentStack** from [Cursor Marketplace](https://cursor.com/marketplace) or copy to `~/.cursor/plugins/local/agentstack/`.
2. In Cursor chat: `/agentstack-init`
3. Docs: `provided_plugins/cursor-plugin/README.md` in the AgentStack monorepo.

Optional slim rules from genetic-ai-starter sync — see `MAINTAINERS.md`.

Gene: `repo.tooling.genetic_starter.gen1`
