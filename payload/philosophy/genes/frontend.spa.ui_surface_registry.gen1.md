# UI surface registry — machine + human catalog

**Genetic tag:** `frontend.spa.ui_surface_registry.gen1`  
**Category:** frontend (SPA IA)  
**Status:** ACTIVE  

Portable export from AgentStack. Pair with `SHELL_MIGRATION_CHECKLIST.md` in this kit.

When adding a route or module: assign `registryId`, maintain PAGES_MAP + snapshot, run `audit:ui-registry`.
