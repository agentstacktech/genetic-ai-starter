/**
 * Feature stub — {{FEATURE}}
 * Genetic tag: {{DOMAIN}}.{{FEATURE}}.feature.gen1
 */
import { getAgentStackSDK } from './agentstack.ts';

/** Entry point for {{FEATURE}} feature logic. */
export function create{{FEATURE_PASCAL}}Client() {
  return getAgentStackSDK();
}
